{
  "name": "npm",
  "version": "1.0.0",
  "description": "Test project voor node packages.$",
  "main": "index.js",
  "scripts": {
    "build": "esbuild app.js --bundle --outfile=build.js",
    "watch": "esbuild app.js --bundle --watch --outfile=build.js",
    "production": "esbuild app.js --bundle --minify --outfile=build.js"
  },
  "keywords": [
    "node",
    "package"
  ],
  "author": "Adriaan Glibert",
  "license": "MIT",
  "dependencies": {
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "esbuild": "^0.19.9",
    "jest": "^29.7.0"
  }
}